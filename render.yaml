services:
  - type: web
    name: telegram-bot
    env: node
    buildCommand: |
      npm install
      pip install -r requirements.txt
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    plan: free
    autoDeploy: true
